// Palindrome Linked List - Pratica 2

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define storageNumbers [MAX_LENGTH];

struct ListNode {
    int val;
    struct ListNode *next;
} bool isPalindrome(struct ListNode *head){
}

typedef pilha {
    int *next;
}storageNumbers; 

int main () {

    int sequencia, i;
    char storageNumbers[];

    printf ( "Faça uma sequencia de numeros seguindo as regras abaixo" ); //regras para receber os números
    printf ( "Digite um numero por vez:\n" );
    printf ( "Digite 111 para sair ou terminar\n" );

    while ( sequencia != 111 ) //dar os numeros de entrada
    {
    printf ( "\nNumero: ");
    scanf ( "%d", &sequencia );
    int *next (int sequencia) -> storageNmubers;
    }
    
for ( i = 0; i < strlength(storageNmubers); i++) //imprimir a sequencia na tela
    printf ( "%d", storageNumbers[i] );
}

//comparar se o último é igual  ao primeiro, e assim oor diante e por fim chamar a funçao para verificar true or false
return 0;
}